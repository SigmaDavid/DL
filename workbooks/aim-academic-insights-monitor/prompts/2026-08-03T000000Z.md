# Prompt of record — 2026-08-03

> Connect to the stridelearning org and create a new workbook template that
> looks like the images in this attached doc. Don't worry about the charts and
> kpis, I am looking just for the navigation images and filters. You can also
> use the attached png for the logo.

Attachment: `AIM_Report_and_Tables.docx` (17 page screenshots of the K12
"Academic Insights Monitor" workbook) + an AIM logo PNG (pasted inline).

## Kickoff answers
- **API access:** Fix egress first — the Sigma REST API host
  `api-us-a.aws.sigmacomputing.com` is blocked by this session's egress policy
  (502 on CONNECT), so no live connect/recon/POST was possible. Deliverable is
  a committed, validated, POST-ready spec.
- **Filters:** Unbound visual shell (styled boxes + labels; no data-model
  binding — segmented nav toggles are functional).
- **Logo:** Placeholder URL + committed extracted PNG
  (`assets/aim-logo.png`).
