# Nothing in this repo reads a .env file — browser sign-in
# (scripts/api/browser-login.sh) and Claude Code web's injected env vars
# cover every auth path (see CLAUDE.md -> "Authentication"). Ignored purely
# as a safety net in case one gets created out of old habit; it should
# never get committed.
.env
.env.*.local

# Personal Claude Code settings (per-user)
.claude/settings.local.json

# Python bytecode cache (generated by any python3/py_compile invocation)
__pycache__/
*.pyc

# Vendor mirror of upstream skills (refresh via scripts/refresh-vendor.sh)
vendor/

# Iteration scratch
workbooks/*/iterations/*.tmp
workbooks/*/iterations/*.draft.json

# Local workbook iteration folders — only _template/ and _exemplars/ are
# repo-tracked; every other workbooks/<name>/ is per-author scratch.
workbooks/*/
!workbooks/_template/
!workbooks/_exemplars/

# Per-user session walkthroughs (not part of the skill itself)
docs/sessions/

# Training session summaries — per-author scratch, often customer-specific
prompts/library/train_*.md

# Phase 1 research artifact — competitor packaging review, internal-only
docs/twells89-review.md

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp

# Note: no token/keychain state is ever written inside the repo tree.
# _env.sh's cache lives at $SIGMA_TOKEN_CACHE (default: a per-user path
# under $XDG_RUNTIME_DIR/$TMPDIR, outside this repo); browser-login.sh's
# refresh token lives in the OS keychain, not a file. The prior
# `.sigma-token` entry here didn't match anything either script actually
# writes and has been removed.
