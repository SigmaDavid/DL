{
  "_comment": [
    "Recommended permissions for a project hosting the sigma-workbook-conventions skill.",
    "Merge the `allow` and `ask` arrays below into your project's `.claude/settings.json`",
    "under `permissions`. With these rules, discovery, inspection, and (after the user",
    "approves the workbook plan) authoring/publishing all run without per-call prompts.",
    "Deletion always prompts — see the `ask` list.",
    "",
    "These scripts are skill-bundled (not workspace-root-relative): invoke",
    "them via ${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/api/*.sh and python3",
    "${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/*.py, which resolve correctly regardless of",
    "the caller's cwd. The allow patterns below match on that fully-resolved",
    "prefix.",
    "",
    "The trailing `mcp__claude_ai_Sigma_Docs__*` entry allowlists the native",
    "Sigma_Docs MCP integration (Claude.ai-side). When that's connected, the",
    "model can look up Sigma function references and REST API endpoint shapes",
    "without bash or WebFetch. Harmless if the integration isn't installed.",
    "",
    "Workspace discovery (finding workbooks/data models) and workbook authoring",
    "stay on the bash path (`${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/api/*`) — they need OAuth bootstrap that",
    "the native MCP doesn't currently provision for downstream users.",
    "",
    "Approval model:",
    "  - Plan-approval authorizes every state-changing API call EXCEPT DELETE.",
    "  - POST/PUT to /v2/workbooks/spec and /v2/dataModels/*/spec run silent.",
    "  - DELETE on ANY endpoint always prompts, regardless of plan content.",
    "  - Future deletion scripts must be named `${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/api/delete-*` so they",
    "    are caught by the ask pattern; the broad Bash(${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/api/*) allow",
    "    would otherwise let them through silently.",
    "",
    "Anything in `ask` MUST stay prompting — destructive ops with no undo.",
    "",
    "The `bash ${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/api/*` allow entry (and its `bash ${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/api/delete-*`",
    "ask-list mirror) exist for filesystems that don't preserve the executable",
    "bit on ${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/api/*.sh — notably a WSL checkout living on a Windows-native",
    "drive mounted via DrvFs (/mnt/c/...). Cloning inside the WSL filesystem",
    "(e.g. ~/...) avoids this entirely; the bash-prefixed entries are a fallback."
  ],
  "permissions": {
    "allow": [
      "Bash(${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/api/*)",
      "Bash(bash ${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/api/*)",
      "Bash(python3 ${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/sigma-resolve.py:*)",
      "Bash(python3 ${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/validate-spec.py:*)",
      "Bash(bash ${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/doctor.sh:*)",
      "Bash(curl * $SIGMA_BASE_URL/v2/dataModels/*)",
      "Bash(curl * $SIGMA_BASE_URL/v2/workbooks/*)",
      "Bash(curl * $SIGMA_BASE_URL/v2/files*)",
      "Bash(curl * $SIGMA_BASE_URL/v2/connections*)",
      "Bash(curl * $SIGMA_BASE_URL/v2/members*)",
      "Bash(curl * $SIGMA_BASE_URL/mcp/v2*)",
      "Bash(echo *)",
      "Bash(ls:*)",
      "Bash(mkdir:*)",
      "Bash(cat:*)",
      "Bash(head:*)",
      "Bash(tail:*)",
      "Bash(jq:*)",
      "Bash(cp:*)",
      "Bash(find:*)",
      "Bash(grep:*)",
      "Bash(git clone:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(python3 -c:*)",
      "mcp__claude_ai_Sigma_Docs__*"
    ],
    "ask": [
      "Bash(curl * -X DELETE *)",
      "Bash(${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/api/delete-*)",
      "Bash(bash ${CLAUDE_PLUGIN_ROOT}/skills/sigma-workbook-conventions/scripts/api/delete-*)"
    ]
  }
}
